import React from "react";

// placeholder component for under development routes
class TestComponent extends React.Component {
    render() {
        return (
            <div>
                {this.props.channel}
            </div>
        );
    }
}

export default TestComponent;
